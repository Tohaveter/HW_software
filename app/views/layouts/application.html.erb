<!DOCTYPE html>
<html>
  <head>
    <title>Senkivtest</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
  <div class="d-flex flex-column flex-md-row align-items-center p3 px-md-4 mb-3 bg-white border-bottom shadow-sm">
  <h5 class="my-0 mr-md-auto font-weight-bold"> Волейбольний турнір </h5>
  <nav class="my-2 my-md-0 mr-md-3">
  <span class="p-2 text-dark"><%= link_to "Головна сторінка", 
 home_path%></span>
  <span class="p-2 text-dark"><%= link_to "Команди", 
 index_path%></span>
  </nav>
  <span class="p-2 text-dark"><%= link_to "Додати команду", 
 new_post_path %></span>
 </div>
 <div class="container">
 <p class="notice"><%= notice %></p> 
<p class="alert"><%= alert %></p> 
<% if current_user %>
<%= current_user.email %>
<%= button_to "Log out", destroy_user_session_path, method: :delete, data: { turbo: false } %>
<% else %>
<% link_to "landing_page", root_path %> 
<% end %>
    <%= yield %>
  </body>
</html>
